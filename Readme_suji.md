## Morse Code Translation 
 Is just matching letters to patterns, we have translation lookup from which we will
 take a word and split into letters lookup to the translation we have and join them and vice versa.


 Logic : 
 just to lookup to the translation we need to convert input to uppercase.
 with get words - split the input with space
 For each word: we separate into letters
 For each letter - we map the morese code in the lookup
 join the letters with "" and to form the word join with "/" and vice versa


 to check what language is input we use if else condition


 
